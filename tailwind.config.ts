/** @type {import('tailwindcss').Config} */
const config = {
    darkMode: 'class', // ⬅️ This enables toggling via a "dark" class
    content: [
      './src/**/*.{js,ts,jsx,tsx,mdx}',
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  };
  export default config;
  